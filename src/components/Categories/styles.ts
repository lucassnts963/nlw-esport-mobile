import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    height: 120,
  },
  content: {
    paddingLeft: 16,
    paddingRight: 32,
    alignItems: 'center',
  },
});
